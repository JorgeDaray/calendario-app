=== CONTEXTO DEL PROYECTO: CALENDARIO DE DISPONIBILIDAD (ACTUALIZADO v14) ===
Stack: HTML puro, JavaScript (FullCalendar 6), CSS (Responsive), Supabase (PostgreSQL).
Host: Vercel.

-- ESTADO DE LA BASE DE DATOS (Supabase) --
Tablas Activas (Con REALTIME habilitado):
1. perfiles, 2. disponibilidad, 3. eventos, 4. asistencia_eventos

-- ESTADO DE LOS ARCHIVOS --
1. index.html: 
   - Sin cambios recientes.
2. style.css: 
   - Sin cambios recientes.
3. app.js: 
   - [ACTUALIZADO] La función de renderizado de `events:` ahora extrae el tipo de vista actual (`info.view.type.includes('list')`). Si es la vista 'Agenda' (lista), la disponibilidad se envía con `display: 'auto'` para forzar a FullCalendar a dibujarla. Si es la vista de cuadrícula, mantiene `display: 'background'`.

===========================================================